{
    "name": "Dev Container Template",
    "dockerComposeFile": "./docker-compose.yml",
    "service": "devcontainer",
    "workspaceFolder": "/workdir",
    "postCreateCommand": "sudo apt-get install -y --no-install-recommends gh curl && sudo chown -R appuser:appgroup /workdir",
    "customizations": {
        "vscode": {
            "extensions": [
                "openai.chatgpt"
            ]
        }
    }
}